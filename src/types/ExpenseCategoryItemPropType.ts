export type ExpenseCategoryItemProps = {
    title : string,
    iconText : string,
    iconColor : string,
    iconHeight? : number,
    iconWidth? : number,
    titleFontSize? : number
}
export type ExpenseCategoryItemType = {
  title: string;
  iconText: string;
  iconColor: string;
};
